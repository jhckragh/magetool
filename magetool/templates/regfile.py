regfile = """<?xml version="1.0"?>
<config>
    <modules>
        <${namespace}_${module_name}>
            <active>true</active>
            <codePool>${code_pool}</codePool>
        </${namespace}_${module_name}>
    </modules>
</config>
"""
